## Packages
date-fns | Date formatting and manipulation for reports
recharts | Charts for visualizing time entry data (optional but good for dashboard)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'Inter'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
}
Authentication uses cookie-based sessions with credentials: "include"
